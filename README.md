# 🏥 Healthcare ML Platform

> An end-to-end machine learning project built on real hospital data — covering Regression, Classification, Clustering, Anomaly Detection, and Recommendation — with a Streamlit dashboard and FastAPI backend.

![Python](https://img.shields.io/badge/Python-3.11-blue?style=flat-square&logo=python)
![Scikit-learn](https://img.shields.io/badge/Scikit--learn-1.3-orange?style=flat-square)
![XGBoost](https://img.shields.io/badge/XGBoost-2.0-red?style=flat-square)
![Streamlit](https://img.shields.io/badge/Streamlit-1.27-ff4b4b?style=flat-square)
![FastAPI](https://img.shields.io/badge/FastAPI-0.103-009688?style=flat-square)
![License](https://img.shields.io/badge/License-MIT-green?style=flat-square)

---

## What this project does

This platform takes two real-world diabetes datasets and turns them into a fully working ML product. A doctor (or interviewer) can open the Streamlit app, enter a patient's details, and immediately get:

- A **readmission risk score** (0–100)
- A **disease prediction** with probability
- A **patient risk segment** (Low / Medium / High)
- An **anomaly flag** if lab values look unusual
- **Treatment recommendations** based on similar past patients
- A **SHAP explanation** of why the model made that decision

---

## Datasets

| Dataset | Source | Used for |
|---|---|---|
| Pima Indians Diabetes | [UCI / Kaggle](https://www.kaggle.com/datasets/uciml/pima-indians-diabetes-database) | Regression, Classification, Clustering, Anomaly Detection |
| Diabetes 130-US Hospitals | [Kaggle](https://www.kaggle.com/datasets/brandao/diabetes) | Recommendation engine |

Download both CSVs and place them in `dataset/raw/` — see Setup below.

---

## ML modules

| Module | Algorithm(s) | Dataset | Metric |
|---|---|---|---|
| Regression | Linear, Ridge, Lasso, XGBoost | Pima | RMSE, R² |
| Classification | Logistic Regression, Random Forest, SVM, XGBoost, LightGBM | Pima | ROC-AUC |
| Clustering | K-Means, DBSCAN, Hierarchical | Pima | Silhouette Score |
| Anomaly Detection | Isolation Forest, LOF, Z-score | Pima | Precision, Recall |
| Recommendation | kNN (content-based) | 130-US | — |
| Explainability | SHAP TreeExplainer | Both | — |

---

## Project structure

```
healthcare-ml-platform/
│
├── Dataset/
│   ├── raw/                          ← Place downloaded CSVs here
│   │   ├── pima_diabetes.csv
│   │   └── 130_diabetic_data.csv
│   └── processed/                    ← Auto-generated by notebooks
│       ├── pima_clean.csv
│       ├── diabetes130_clean.csv
│       └── treatment_lookup.csv
│
├── notebooks/                        ← Run in order
│   ├── 01_EDA_pima.ipynb
│   ├── 02_EDA_diabetes130.ipynb
│   ├── 03_feature_engineering.ipynb
│   ├── 04_regression.ipynb
│   ├── 05_classification.ipynb
│   ├── 06_clustering.ipynb
│   ├── 07_anomaly_detection.ipynb
│   ├── 08_recommendation.ipynb
│   ├── 09_shap_explainability.ipynb
│   └── 10_risk_scoring.ipynb
│
├── src/                              ← Reusable Python modules
│   ├── preprocess.py                 ← Cleaning and imputation
│   ├── features.py                   ← Feature engineering pipeline
│   ├── train.py                      ← Training and evaluation utilities
│   ├── compare_models.py             ← 5-algorithm comparison table
│   ├── explain.py                    ← SHAP wrapper
│   ├── risk_engine.py                ← Risk score 0–100 + alert bands
│   └── predict.py                    ← Inference for API and app
│
├── models/                           ← Saved .pkl files after training
│
├── app/                              ← Streamlit multi-page dashboard
│   ├── Home.py
│   └── pages/
│       ├── 1_EDA.py
│       ├── 2_Predict.py
│       ├── 3_Compare.py
│       ├── 4_Explain.py
│       └── 5_Recommend.py
│
├── api/                              ← FastAPI REST endpoints
│   ├── main.py
│   └── schemas.py
│
├── outputs/                          ← Auto-saved plots from notebooks
├── tests/
├── requirements.txt
└── README.md
```

---

## Setup

### 1. Clone the repository

```bash
git clone https://github.com/your-username/healthcare-ml-platform.git
cd healthcare-ml-platform
```

### 2. Create a conda environment with Python 3.11

```bash
conda create -n healthcare python=3.11 -y
conda activate healthcare
```

### 3. Install dependencies

```bash

pip install -r requirements.txt
```


### 5=4. Download the datasets

| File | Download link | Save as |
|---|---|---|
| Pima Indians | [Kaggle link](https://www.kaggle.com/datasets/uciml/pima-indians-diabetes-database) | `Dataset/raw/pima_diabetes.csv` |
| 130-US Hospitals | [Kaggle link](https://www.kaggle.com/datasets/brandao/diabetes) | `Dataset/raw/130_diabetic_data.csv` |

---

## Running the notebooks

Open Jupyter and select the **Healthcare ML** kernel:

```bash
jupyter notebook
```

Run notebooks in order — each one saves its output to `Dataset/processed/` or `models/` for the next notebook to use:

```
01_EDA_pima           →  Dataset/processed/pima_clean.csv
02_EDA_diabetes130    →  Dataset/processed/diabetes130_clean.csv
                          Dataset/processed/treatment_lookup.csv
03_feature_eng        →  fitted pipeline objects
04_regression         →  models/regression_xgb.pkl
05_classification     →  models/classification_rf.pkl
                          models/classification_xgb.pkl
06_clustering         →  models/kmeans.pkl
07_anomaly            →  models/isolation_forest.pkl
08_recommendation     →  models/knn_recommender.pkl
09_shap               →  SHAP plots in outputs/
10_risk_scoring       →  risk score logic
```

---

## Running the Streamlit app

Make sure you have run all notebooks first so the model `.pkl` files exist in `models/`.

```bash
streamlit run app/Home.py
```

Opens at `http://localhost:8501`

### App pages

| Page | What it does |
|---|---|
| Home | Project overview and dataset stats |
| EDA | Interactive dataset explorer with filters and live plots |
| Predict | Enter patient values → get risk score + disease prediction |
| Compare | Model leaderboard — ROC-AUC comparison of all 5 classifiers |
| Explain | SHAP waterfall plot — why did the model make that decision? |
| Recommend | Enter patient profile → get treatment suggestions |

---

## Running the API

```bash
uvicorn api.main:app --reload
```

API docs at `http://localhost:8000/docs`

### Endpoints

```
POST  /predict/risk           →  Readmission risk score (0–100)
POST  /predict/disease        →  Diabetic / not diabetic + probability
POST  /predict/segment        →  Cluster: Low / Medium / High risk
POST  /predict/anomaly        →  Unusual lab values flag
POST  /recommend/treatment    →  Top medication suggestions
```

### Example request

```bash
curl -X POST "http://localhost:8000/predict/risk" \
     -H "Content-Type: application/json" \
     -d '{
           "pregnancies": 2,
           "glucose": 148,
           "blood_pressure": 72,
           "skin_thickness": 35,
           "insulin": 0,
           "bmi": 33.6,
           "diabetes_pedigree_function": 0.627,
           "age": 50
         }'
```

### Example response

```json
{
  "risk_score": 74.3,
  "risk_level": "High",
  "recommendation": "Flag for follow-up within 48 hours"
}
```

---

## Key design decisions

**Why classical ML and not deep learning?**
Tree-based models (XGBoost, Random Forest) consistently outperform neural networks on tabular medical data. They are also faster to train, easier to interpret with SHAP, and require far less data. Deep learning will be added as a future extension.

**Why two datasets?**
The Pima dataset has diagnostic measurements but no treatment history, we cannot recommend medication from it. The 130-US dataset has medication columns (`insulin`, `metformin` etc.) that make recommendation meaningful.

**Why median imputation for zeros in Pima?**
Columns like `Glucose` and `BMI` use `0` as a placeholder for missing data. We replace zeros with `NaN` then impute with the column median rather than mean because medical measurements are typically skewed.

**Why ROC-AUC over accuracy?**
The Pima dataset is 65/35 imbalanced. A model that always predicts "no diabetes" gets 65% accuracy but is clinically useless. ROC-AUC of 0.50 correctly identifies this as no better than random guessing.

---

## Results summary

| Model | Type | ROC-AUC | RMSE |
|---|---|---|---|
| XGBoost | Classification | - | — |
| LightGBM | Classification | -- | — |
| Random Forest | Classification | -- | — |
| XGBoost | Regression | — | -- |
| K-Means (K=3) | Clustering | Silhouette: -- | — |

---

## Tech stack

| Category | Libraries |
|---|---|
| Data | pandas, numpy |
| Machine Learning | scikit-learn, xgboost, lightgbm |
| Visualisation | matplotlib, seaborn, plotly |
| Explainability | shap |
| API | fastapi, uvicorn, pydantic |
| Dashboard | streamlit |
| Model persistence | joblib |

---

## Roadmap

- [ ] Add LSTM time-series module for readmission forecasting
- [ ] Dockerise the API and Streamlit app
- [ ] Add unit tests for `src/` modules
- [ ] Deploy Streamlit app to Streamlit Cloud
- [ ] Add a patient report PDF export

---

## License

MIT License — free to use, modify, and distribute with attribution.

---

## Author

Built as a portfolio project to demonstrate end-to-end ML engineering on real healthcare data.  
Feel free to open issues or pull requests.
